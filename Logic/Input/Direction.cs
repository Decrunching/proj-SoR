namespace SoR.Logic.Input
{
    /*
     * Calculate directional input logic.
     */
    public abstract class Direction
    {
        protected float directionX;
        protected float directionY;
        protected string direction;

        /*public string GetDirection(float newPositionX, float prevPositionX, float newPositionY, float prevPositionY)
        {
            directionX = newPositionX - prevPositionX;
            directionY = newPositionY - newPositionY;

            if (directionX != 0 | directionY != 0)
            {
                if ()
                {
                }
            }

            return direction;
        }*/
    }
}