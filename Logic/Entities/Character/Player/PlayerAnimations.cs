namespace Logic.Entities.Character.Player
{
    /*
     * Placeholder class for storing additional fields for animation dictionary, as player
     * and non-player characters all likely to end up with more than one animation to switch
     * through per change event.
     */
    internal class PlayerAnimations
    {
        public string animOne;
        public string animTwo;

        public PlayerAnimations()
        {
        }
    }
}